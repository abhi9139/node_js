name: Artifacts
on: push
jobs:  
  # job1:
  #   runs-on: ubuntu-latest

  #   steps:
  #    - name: Make a drectory
  #      run: mkdir build

  #    - name: Create a file 
  #      run: echo "This is first file "  > build/test.txt

  #    - name: Upload Artifact
  #      uses: actions/upload-artifact@v4
  #      with:
  #        name: First_Artifact
  #        path: build/

  job2:
    # needs: job1
    runs-on: ubuntu-latest

    steps:
     - name: Download Artifact
       uses: actions/download-artifact@v4
       with:
        name: First_Artifact
        path: ./build

     - name: Display Download Files
       run: |
        ls -lart ./build 
        cat ./build/test.txt


  


 
  